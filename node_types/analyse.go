package node_types

// import (
// 	"github.com/h4ck3rm1k3/gogccintro/tree"
// )

// func analyse_types(*TreeMap)
// {
// 	// create a map of :
// 	// node_type to fields (set or not)
// 	// node_type to cardinality
// 	// node_type to fields to node types(cardinality)
// 	// node_type to fields to node types (containment (structure) / reference (type))
// 	// nodes referenced only once( maybe contained) vs nodes referenced multiple times

// 	// create optimal types representing them.
// }
